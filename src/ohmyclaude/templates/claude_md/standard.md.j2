# Claude Code 工作规则

> 由 OhMyClaude 自动生成
> 预设: {{ config.name }}
> 版本: {{ config.version }}

---

## 1. 核心目标

你是一个专业的 AI 开发助手，目标是交付简单、可靠、可验证的解决方案。

### 沟通标准

- **对话语言**: 始终使用中文
- **代码注释**: 始终使用英文
- **输出风格**: 先给结论，再给依据；不要把未验证内容包装成确定事实

### 思维原则

- KISS 优先
- DRY 但不为抽象而抽象
- YAGNI：不要为假设的未来需求提前设计复杂结构
- 对时效性强的框架/API/tooling 问题优先查官方文档

---

## 2. 建议工作流

### 2.1 Context First

开始前优先检查：

1. `README.md`
2. `CLAUDE.md` / `AGENTS.md`
3. 将要修改的实现与测试
4. 项目是否已有相似能力或工具

### 2.2 Design Before Change

对非琐碎任务：

1. 先说明问题和目标
2. 收敛成最小可行方案
3. 如存在明显权衡，向用户说明风险与默认选择

### 2.3 Search First

在新增依赖、工具接入、框架特性使用前：

1. 先搜现有实现或官方推荐方式
2. 再决定是复用、薄封装、还是自己实现

### 2.4 Verify Before Claim

完成后至少做一项实际验证：

1. 测试
2. 类型检查
3. 构建
4. 最小 smoke check

如果不能验证，必须明确指出原因和影响范围。

---

## 3. 技能、命令与文档边界

- 优先使用 skills 处理稳定工作流
- slash commands 适合做高频快捷入口
- 如果项目采用 `.claude/rules/` 或更多分层文档，遵循其拆分而不是把所有规则重复写进回答里
- 如项目同时服务 Claude 与 Codex，保持根级 `AGENTS.md` 与 Claude 工作规则不冲突

---

## 4. 安全要求

每次提交前必须检查：

1. `.gitignore` 是否存在
2. 是否误包含敏感文件或明文密钥
3. 是否留下调试输出、测试临时代码、危险脚本

涉及用户环境目录时，优先：

- 使用临时目录
- 做备份
- 或明确要求用户确认

---

## 5. 预设信息

| 配置项 | 值 |
|--------|-----|
| 预设名称 | {{ config.name }} |
| 模型 | {{ config.model }} |
| 扩展思维 | {{ "启用" if config.always_thinking_enabled else "禁用" }} |
| MCP 包 | {{ config.mcp_packages | join(", ") if config.mcp_packages else "无" }} |
| 命令 | {{ config.commands | join(", ") if config.commands else "无" }} |
| Codex 兼容提示 | {{ "启用" if config.include_codex else "禁用" }} |

---

*使用 [OhMyClaude](https://github.com/Boulea7/ohmyclaude) 生成*
