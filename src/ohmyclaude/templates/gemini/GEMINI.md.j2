# OhMyClaude Gemini Context

This extension was generated by OhMyClaude for the `{{ preset.name }}` preset.

## Defaults

- Keep changes explicit and easy to review.
- Prefer repository-local edits and generated assets over touching real user home-directory AI config.
- Validate meaningful changes before declaring success.

## Recommended Skills

{% for skill_name in portable_skills %}
- `{{ skill_name }}`
{% endfor %}

## Safety Notes

- This bundle is intended to be installed into an explicit extension directory.
- It does not merge or rewrite `~/.gemini/settings.json`.
- Review `hooks/hooks.json` and generated commands before enabling them in a shared environment.
